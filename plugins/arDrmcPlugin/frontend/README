Built with AngularJS!

This could be in a different repo some day?

The directory structure is based in the angular generator found in yeoman.

Build instructions:

# sudo apt-get install build-essential // yep, you're going to need make, gcc etc...
# npm install
# grunt build
