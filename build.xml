<?xml version="1.0"?>
<project name="atom" basedir="." default="build">

  <!-- ===================================================================== -->
  <!-- Define some globals                                                   -->
  <!-- ===================================================================== -->

  <property name="build.dir" value="${phing.dir}/build"/>
  <property name="package.dir" value="${build.dir}/package"/>

  <!-- package.version: e.g. 2.10.2 -->
  <exec executable="sed" outputProperty="package.version" checkreturn="true">
    <arg value="-n"/>
    <arg value="s/.*public const VERSION = '\([^']*\)'.*/\1/p"/>
    <arg value="src/Framework/Bridge/RuntimeConfiguration.php"/>
  </exec>

  <!-- package.hash: e.g. e074179cb205a43b20ac171b9917d1608a41b5dd -->
  <exec executable="git" outputProperty="package.hash" checkreturn="true">
    <arg line="rev-parse HEAD"/>
  </exec>
  <exec executable="git" outputProperty="package.hash16" checkreturn="true">
    <arg line="rev-parse --short=16 HEAD"/>
  </exec>

  <!-- package.pear.name: e.g. atom-2.0.0 -->
  <property name="package.pear.name" value="${phing.project.name}-${package.version}"/>

  <!-- package.name: e.g. atom-2.0.0-20030101-e6471c09494dd6fe or atom-2.0.0 -->
  <exec executable="git" outputProperty="package.tag" checkreturn="true">
    <arg line="tag --points-at HEAD --list v${package.version}"/>
  </exec>
  <if>
    <equals arg1="${package.tag}" arg2="v${package.version}"/>
    <then>
      <property name="package.name" value="${package.pear.name}"/>
    </then>
    <else>
      <exec executable="date" outputProperty="date">
        <arg value="+%Y%m%d"/>
      </exec>
      <property name="package.name" value="${package.pear.name}-${date}-${package.hash16}"/>
    </else>
  </if>

  <!-- ===================================================================== -->
  <!-- Debug target                                                          -->
  <!-- ===================================================================== -->

  <target name="debug">
    <echo>Debug:</echo>
    <echo>  - phing.dir:           ${phing.dir}</echo>
    <echo>  - phing.file:          ${phing.file}</echo>
    <echo>  - build.dir:           ${build.dir}</echo>
    <echo>  - package.version:     ${package.version}</echo>
    <echo>  - package.revision:    ${package.hash}</echo>
    <echo>  - package.name:        ${package.name}</echo>
  </target>

  <!-- ===================================================================== -->
  <!-- Build target                                                          -->
  <!-- ===================================================================== -->

  <target name="build"
    depends="debug, prepare, package"
    description="Build a tarball"/>

  <target name="verify">
    <exec executable="git" outputProperty="working.tree.status" checkreturn="true">
      <arg line="status --porcelain --untracked-files=normal"/>
    </exec>
    <if>
      <not>
        <equals arg1="${working.tree.status}" arg2=""/>
      </not>
      <then>
        <fail message="Commit or remove working-tree changes before building a release."/>
      </then>
    </if>
  </target>

  <target name="prepare" depends="debug, verify">
    <delete dir="${build.dir}"/>
    <mkdir dir="${package.dir}"/>

    <echo>Export committed source tree</echo>
    <exec executable="sh" dir="${phing.dir}" checkreturn="true">
      <arg value="-c"/>
      <arg value="git archive --format=tar HEAD | tar -xf - -C &quot;${package.dir}&quot;"/>
    </exec>

    <echo>Install production dependencies with composer</echo>
    <exec executable="composer" dir="${package.dir}" checkreturn="true">
      <arg line="install --no-dev --no-interaction --no-progress --prefer-dist"/>
    </exec>

    <echo>Build Bootstrap 5 theme</echo>
    <exec executable="npm" dir="${package.dir}" checkreturn="true">
      <arg value="ci"/>
    </exec>
    <exec executable="env" dir="${package.dir}" checkreturn="true">
      <arg value="NODE_ENV=production"/>
      <arg value="npm"/>
      <arg value="run"/>
      <arg value="build"/>
    </exec>
    <delete dir="${package.dir}/node_modules"/>

    <echo>Run symfony release task</echo>
    <exec executable="php" dir="${package.dir}" checkreturn="true">
      <arg line="symfony release --no-confirmation ${package.version} stable"/>
    </exec>
  </target>

  <target name="package" depends="prepare">
    <echo>Building pear package</echo>
    <exec executable="pear" dir="${package.dir}" checkreturn="true">
      <arg value="package"/>
    </exec>
    <move file="${package.dir}/${package.pear.name}.tgz" tofile="${build.dir}/${package.name}.tar.gz" overwrite="true"/>
    <delete dir="${package.dir}"/>
    <echo>Done! ${build.dir}/${package.name}.tar.gz</echo>
  </target>

  <!-- ===================================================================== -->
  <!-- i18n shared targets                                                   -->
  <!-- ===================================================================== -->

  <target name="i18n-prepare">
    <mkdir dir=".tx/i18n"/>
    <exec executable="git" returnProperty="return_code">
      <arg line="diff --quiet"/>
    </exec>
    <if>
      <equals arg1="${return_code}" arg2="0" />
      <then>
        <echo>You are good to go!</echo>
      </then>
      <else>
        <input propertyName="cont" defaultValue="n" promptChar="?" validArgs="y,n">
          You have unstaged changes in your git tree. Are you sure you want to continue?
        </input>
        <if>
          <equals arg1="${cont}" arg2="n"/>
          <then>
            <fail message="Exiting without changes..."/>
          </then>
        </if>
      </else>
    </if>
  </target>

  <target name="i18n-clean-tx">
    <exec executable="sh">
      <arg value="-c"/>
      <arg value="rm -rf .tx/*"/>
    </exec>
  </target>

  <target name="i18n-extract">
    <foreach param="lang" target="i18n-extract-subtask">
      <fileset dir="apps/qubit/i18n/">
        <type type="dir"/>
        <depth max="0" min="0"/>
      </fileset>
    </foreach>
  </target>
  <target name="i18n-extract-subtask">
    <exec executable="php" dir="${phing.dir}" checkreturn="true">
      <arg line="symfony i18n:extract --plugins --auto-delete --auto-save ${lang}"/>
    </exec>
  </target>

  <!-- ===================================================================== -->
  <!-- i18n-push-sources target, extract 'en' source strings to XLIFF        -->
  <!-- ===================================================================== -->

  <target name="i18n-push-sources"
    depends="i18n-clean-tx, i18n-prepare, i18n-extract-sources, i18n-consolidate-sources, i18n-cleanup-sources"
    description="Extract 'en' source strings from AtoM"/>

  <target name="i18n-extract-sources">
    <exec executable="php" dir="${phing.dir}" checkreturn="true">
      <arg line="symfony i18n:extract --plugins --auto-delete --auto-save en"/>
    </exec>
  </target>

  <target name="i18n-consolidate-sources">
    <exec executable="php" dir="${phing.dir}" checkreturn="true">
      <arg line="symfony i18n:consolidate en .tx/i18n/"/>
    </exec>
  </target>

  <target name="i18n-cleanup-sources">
    <exec executable="sh">
      <arg value="-c"/>
      <arg value="find . -not -path &quot;*/\.*&quot; -wholename &quot;*i18n/en&quot; -not \( -name &quot;.tx&quot; -prune \) | xargs -IF rm -rf F"/>
    </exec>
  </target>

  <!-- ===================================================================== -->
  <!-- i18n-push-translations target, extract translations to XLIFF          -->
  <!-- ===================================================================== -->

  <target name="i18n-push-translations"
    depends="i18n-clean-tx, i18n-cleanup-create-plugin-i18n-folders-if-missing, i18n-prepare, i18n-extract, i18n-consolidate-translations"
    description="Extract translations from AtoM"/>

  <target name="i18n-consolidate-translations">
    <foreach param="lang" target="i18n-consolidate-translations-subtask">
      <fileset dir="apps/qubit/i18n/">
        <type type="dir"/>
        <depth max="0" min="0"/>
      </fileset>
    </foreach>
  </target>
  <target name="i18n-consolidate-translations-subtask">
    <exec executable="php" dir="${phing.dir}" checkreturn="true">
      <arg line="symfony i18n:consolidate ${lang} .tx/i18n/"/>
    </exec>
  </target>

  <!-- ===================================================================== -->
  <!-- i18n-pull-translations target, import translations from .tx folder    -->
  <!-- ===================================================================== -->

  <target name="i18n-pull-translations"
    depends="i18n-prepare, i18n-fetch, i18n-rectify, i18n-update-fixtures, i18n-cleanup"
    description="Import translations into AtoM, ready to be committed"/>

  <target name="i18n-fetch">
    <foreach param="lang" absparam="directory" target="i18n-fetch-subtask">
      <fileset dir=".tx/i18n">
        <type type="dir"/>
        <depth max="0" min="0"/>
      </fileset>
    </foreach>
  </target>
  <target name="i18n-fetch-subtask">
    <copy file="${directory}/messages.xml" tofile="apps/qubit/i18n/${lang}/messages.xml" overwrite="true"/>
  </target>

  <target name="i18n-rectify">
    <foreach param="lang" target="i18n-rectify-subtask">
      <fileset dir="apps/qubit/i18n/">
        <type type="dir"/>
        <depth max="0" min="0"/>
      </fileset>
    </foreach>
  </target>
  <target name="i18n-rectify-subtask">
    <exec executable="php" dir="${phing.dir}" checkreturn="true">
      <arg line="symfony i18n:rectify ${lang}"/>
    </exec>
  </target>

  <target name="i18n-update-fixtures">
    <exec executable="php" dir="${phing.dir}" checkreturn="true">
      <arg line="symfony i18n:update-fixtures apps/qubit/i18n/"/>
    </exec>
  </target>

  <target name="i18n-cleanup" depends="i18n-cleanup-create-plugin-i18n-folders-if-missing">
    <!-- The extract task is part of Symfony, but fails if a plugin doesn't have an i18n subdirectory -->
    <foreach param="lang" target="i18n-cleanup-extract-subtask">
      <fileset dir="apps/qubit/i18n/">
        <type type="dir"/>
        <depth max="0" min="0"/>
      </fileset>
    </foreach>
    <foreach param="plugin" target="i18n-cleanup-remove-empty-plugin-subdirs-subtask">
      <fileset dir="plugins/">
        <type type="dir"/>
        <depth max="0" min="0"/>
      </fileset>
    </foreach>
  </target>
  <target name="i18n-cleanup-extract-subtask">
    <exec executable="php" dir="${phing.dir}" checkreturn="true">
      <arg line="symfony i18n:extract --auto-save --auto-delete --plugins ${lang}"/>
    </exec>
  </target>
  <target name="i18n-cleanup-remove-empty-plugin-subdirs-subtask">
    <exec executable="rmdir" dir="${phing.dir}">
      <arg value="plugins/${plugin}/i18n"/>
    </exec>
  </target>

  <target name="i18n-cleanup-create-plugin-i18n-folders-if-missing">
    <foreach param="plugin" target="i18n-cleanup-create-plugin-subdirs-subtask">
      <fileset dir="plugins/">
        <type type="dir"/>
        <depth max="0" min="0"/>
      </fileset>
    </foreach>
  </target>
  <target name="i18n-cleanup-create-plugin-subdirs-subtask">
    <exec executable="mkdir" dir="${phing.dir}">
      <arg value="plugins/${plugin}/i18n"/>
    </exec>
  </target>

</project>
